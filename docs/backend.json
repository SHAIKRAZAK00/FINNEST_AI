
{
  "entities": {
    "Family": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Family",
      "type": "object",
      "description": "Represents a secure family unit within the FinNest AI ecosystem, sharing financial data and goals.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Family entity."
        },
        "familyName": {
          "type": "string",
          "description": "A user-friendly name for the family (e.g., 'The Smith Family')."
        },
        "familyCode": {
          "type": "string",
          "description": "A unique, short code generated for members to use when joining the family."
        },
        "createdByUserId": {
          "type": "string",
          "description": "Reference to the User ID (from Firebase Auth) of the parent who initially created this family. (Relationship: User 1:N Family)"
        },
        "members": {
          "type": "object",
          "description": "A denormalized map of userId to their role and join status, used for fast authorization checks in Security Rules.",
          "additionalProperties": {
            "type": "object",
            "properties": {
              "role": { "type": "string" },
              "joinedAt": { "type": "string", "format": "date-time" }
            }
          }
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the family record was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "familyName",
        "familyCode",
        "createdByUserId",
        "members",
        "createdAt"
      ]
    },
    "FamilyMember": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "FamilyMember",
      "type": "object",
      "description": "Represents a specific user's membership within a family, defining their role and join date.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this FamilyMember record, typically the user's ID from Firebase Auth."
        },
        "familyId": {
          "type": "string",
          "description": "Reference to the Family this member belongs to. (Relationship: Family 1:N FamilyMember)"
        },
        "userId": {
          "type": "string",
          "description": "The unique identifier of the user from Firebase Authentication."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the family (e.g., 'Parent', 'Child', 'Viewer')."
        },
        "joinedAt": {
          "type": "string",
          "description": "Timestamp when the user joined the family.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "familyId",
        "userId",
        "role",
        "joinedAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/families/{familyId}",
        "definition": {
          "entityName": "Family",
          "schema": {
            "$ref": "#/backend/entities/Family"
          },
          "description": "Top-level collection for family units. Each document represents a family and includes a denormalized 'members' map (userId -> {role, joinedAt}) for direct family-level authorization checks and as the source of truth for membership.",
          "params": [
            {
              "name": "familyId",
              "description": "The unique identifier for the family."
            }
          ]
        }
      }
    ]
  }
}
